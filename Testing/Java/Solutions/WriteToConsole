Example from writing input to console in Java

import java.util.Scanner;

public class WriteToConsole {
	public static void main(String[] args){
		/*
			Method 1
			Uses the readLine() method for reading in information
			readLine needs the console to work
			
			System.out.println("What is your first name");
			String fname = System.console().readLine();
			System.out.println("What is your last name");
			String lname = System.console().readLine();
			
			System.out.println("Your full name is : " + fname + " " + lname);
			
			String name = fname + " " + lname;
			System.out.println("Hi " + name);
		*/
		
		/* 
			Method 2
			Uses the scanner class for reading in information
			
			Scanner myScan = new Scanner(System.in);
			System.out.println("What is your name");
			
			String name = myScan.nextLine();	
			
			String formattedName = name;
			String[] stringArray; 
			String spliter = "\\s+";
			stringArray = formattedName.split(spliter, 1);
			
			for(int i = 1; i < stringArray.length; i++) {
				String fname = stringArray[0];
				String lname = stringArray[1];
				
				System.out.println("Your name is : " + fname + " " + lname);
			} 
		*/
	
	}
} 
