Given a String array:
String[] words = { "Peach", "Orange", "Mango", "Cherry", "Apple" };

Write code, using for loops to sort this array in lexicographic order and store in another array. Output the results to the console

package org.java.exercisefourqthree;

import java.util.*;

public class SortedFruitsArray {
	public static void main(String[] args){
		String[] fruits = {
			"Peach", "Orange", "Mango", "Cherry", "Apple" 
		};

		
		try {
			
		} catch (ArithmeticException | InputMismatchException ex){ 
			System.out.println("Exception: " + ex.toString());
		}
	}
}
